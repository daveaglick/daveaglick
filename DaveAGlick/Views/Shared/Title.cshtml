@model string

@using (var row = Bs.GridRow().AddCss("banner").Begin())
{
    using (row.GridColumn().SetSm(12).Begin())
    { 
        @Bs.Div().AddCss("title", Model).SetText(" " + ViewData[ViewDataKeys.Title])
    }
}
<hr />